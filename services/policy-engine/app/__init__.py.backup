"""Policy engine package.

Import the central path‑setup shim to ensure the repository root is on
``sys.path`` for top‑level imports such as ``common``.
"""

import services._path_setup  # noqa: F401
