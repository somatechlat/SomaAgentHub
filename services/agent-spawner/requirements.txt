fastapi>=0.110.0
uvicorn[standard]>=0.30.0
kubernetes>=30.1.0
sqlmodel>=0.0.21
asyncpg>=0.29.0
hvac>=2.2.0
# The orchestrator package provides shared DB utilities and models.
# It is part of the same monorepo, so we reference it via a relative path.
-e ./services/orchestrator