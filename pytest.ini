[pytest]
# Limit test discovery to core orchestrator and top-level tests to avoid import errors from unrelated services during CI runs.
testpaths =
    tests
    services/orchestrator/tests
addopts = -q